echo "Running checks before commit..."

# Check Backend
echo "Linting Backend..."
cd webiu-server && pnpm exec lint-staged || exit 1
cd ..

# Check Frontend
echo "Linting Frontend..."
cd webiu-ui && pnpm exec lint-staged || exit 2

echo "All checks passed!"
